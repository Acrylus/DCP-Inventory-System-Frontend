// Settings.tsx
const Settings = () => {
    return (
        <div>
            <h1>Settings</h1>
            {/* Add content for system settings */}
        </div>
    );
};

export default Settings;
