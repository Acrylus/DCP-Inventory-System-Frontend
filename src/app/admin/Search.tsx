// Search.tsx
const Search = () => {
    return (
        <div>
            <h1>Search</h1>
            {/* Add content for searching schools, users, etc. */}
        </div>
    );
};

export default Search;
